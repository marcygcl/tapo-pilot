.env
__pycache__/
*.pyc
.DS_Store
# Token cache de Emporia (pyemvue)
.emporia_tokens.json
**/.emporia_tokens.json

# Stata/binarios de datos pesados
data/*.dta

# Diagnósticos locales (benchmark + discovery) — contienen internos del dispositivo
# (WiFi SSID/BSSID/MAC/IP, geolocalización de cuenta); NO deben auto-commitearse.
energy_logs/benchmark/
