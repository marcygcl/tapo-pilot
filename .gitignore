.env
__pycache__/
*.pyc
.DS_Store
# Token cache de Emporia (pyemvue)
.emporia_tokens.json
**/.emporia_tokens.json

# Stata/binarios de datos pesados
data/*.dta

# Diagnósticos locales en benchmark/ — discovery dumps con internos del dispositivo
# (WiFi SSID/BSSID/MAC/IP, geolocalización). NO commitear, EXCEPTO los CSV de
# resiliencia (esquema sin datos sensibles).
energy_logs/benchmark/*
!energy_logs/benchmark/resilience_test_*.csv

# Script de debug Tapo con credenciales hardcodeadas — solo local, nunca commitear.
scripts/test_tapo_cloud.py
